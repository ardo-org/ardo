[tools]
# ardo is installed via GitHub Releases binary

[settings]
# See: https://github.com/ardo-org/ardo/releases

[tasks.install]
description = "Install ardo globally via deno"
run = "deno install --global --allow-all -n ardo --config deno.json --force src/cli/main.ts"
